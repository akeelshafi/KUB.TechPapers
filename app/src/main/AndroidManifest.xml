<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission for internet access -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KUBTechPapers"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.KUBTechPapers"> <!-- Apply theme -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Result Activity -->
        <activity
            android:name=".ResultActivity"
            android:exported="true"
            android:theme="@style/Theme.KUBTechPapers" /> <!-- Apply theme -->

        <!-- Syllabus Activity -->
        <activity
            android:name=".SyllabusActivity"
            android:exported="true"
            android:theme="@style/Theme.KUBTechPapers" /> <!-- Apply theme -->

        <!-- Engineering Branches Activity -->
        <activity
            android:name=".EngineeringBranchesActivity"
            android:exported="true"
            android:theme="@style/Theme.KUBTechPapers" /><!-- Apply theme -->

        <!-- Metadata for preloaded fonts -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
