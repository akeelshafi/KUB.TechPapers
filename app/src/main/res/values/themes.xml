<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.KUBTechPapers" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Toolbar and StatusBar color -->
        <item name="colorPrimary">@color/toolbar_color</item> <!-- Toolbar color -->
        <item name="colorPrimaryDark">@color/toolbar_color</item> <!-- StatusBar color -->
        <item name="android:statusBarColor">@color/toolbar_color</item> <!-- StatusBar for older versions -->
        <item name="colorAccent">@color/white</item> <!-- Accent color -->
    </style>

    <!-- Main theme -->
    <style name="Theme.KUBTechPapers" parent="Base.Theme.KUBTechPapers" />
</resources>
